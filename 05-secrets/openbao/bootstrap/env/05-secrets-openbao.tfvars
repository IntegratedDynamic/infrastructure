# No overrides needed yet — token_ttl_seconds / token_max_ttl_seconds /
# secret_id_ttl_days all default sensibly (see variables.tf). This file exists
# to name the terraform workspace ("05-secrets-openbao"), per repo convention.
